apiVersion: v1
kind: Service
metadata:
  name: demo-app
  namespace: demo-app
  labels:
    app: demo-app
spec:
  ports:
  - port: 8080
    targetPort: 8080
    name: metrics
    protocol: TCP
  selector:
    app: demo-app
